import styled from 'styled-components';

import MainTitle from '../Title';

// selecionando styled componet para ser modificado
export const Title = styled(MainTitle)` // herdando as propriedade e stilos porem modificando a cor
  color: #F3937F;
`;
